# React will inline env vars prefixed with REACT_APP_ at build time.
# Set this to your FastAPI backend base URL.
# Example (local): http://localhost:3001
# Example (kavia): https://<your-backend-host>
REACT_APP_API_BASE_URL=
